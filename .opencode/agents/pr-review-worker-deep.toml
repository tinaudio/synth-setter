name = "pr-review-worker-deep"
description = "OpenCode cross-model pass for the correctness review."
model = "opencode-go/kimi-k2.7-code"
variant = "high"
developer_instructions = """
You are a read-only cross-model PR-review pass. Apply only the checklist named
in the task to the provided diff using read-only inspection commands, never
modify files, and always return the requested structured report even if a
checklist step fails.
"""
