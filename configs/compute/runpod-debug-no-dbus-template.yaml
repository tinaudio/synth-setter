# SkyPilot task template for RunPod — DEBUG variant 4: branch wrapper
# without dbus-run-session. See
# scripts/run-linux-vst-headless-no-dbus.sh for the hypothesis.

resources:
  cloud: runpod
  accelerators:
    {
      RTX3070:1,
      RTX3080:1,
      RTX3090:1,
      RTXA4000:1,
      RTX4000Ada:1,
      A40:1,
      RTX4090:1,
    }
  use_spot: false
  disk_size: 50
  image_id: docker:tinaudio/synth-setter:dev-snapshot

envs:
  RCLONE_CONFIG_R2_TYPE: ""
  RCLONE_CONFIG_R2_PROVIDER: ""
  RCLONE_CONFIG_R2_ACCESS_KEY_ID: ""
  RCLONE_CONFIG_R2_SECRET_ACCESS_KEY: ""
  RCLONE_CONFIG_R2_ENDPOINT: ""

setup: |
  set -euo pipefail
  cd /tmp
  rm -rf synth-setter-branch
  git clone --depth 1 --branch feat/skypilot-runpod-smoke \
    https://github.com/tinaudio/synth-setter.git synth-setter-branch
  echo "synth-setter@feat/skypilot-runpod-smoke = $(git -C synth-setter-branch rev-parse HEAD)"

run: |
  set -euo pipefail
  cd /tmp/synth-setter-branch
  scripts/run-linux-vst-headless-no-dbus.sh echo "skypilot-debug variant=no-dbus done (host: $(hostname))"
