_target_: src.data.surge_datamodule.SurgeDataModule
dataset_root: ???
use_saved_mean_and_variance: true
batch_size: 128
ot: true
num_workers: 11
predict_file: ???

# Datagen-only — see comment in surge.yaml.
train_val_test_sizes: [440000, 20000, 20000]
train_val_test_seeds: [42, 43, 44]
