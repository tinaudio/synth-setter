# Staging-only variant of the network-volume template: the copy writes R2 ->
# mounted volume, so it needs no GPU and no 750 GB local disk. CPU pods are
# the schedulability win — their stock stays High while every GPU tier runs
# Low. RunPod caps CPU-instance container disks at 40 GB (#2197).
resources:
  cloud: runpod
  instance_type: cpu3c-8-16
  use_spot: false
  disk_size: 40

volumes:
  /workspace/network-volume: ${NETWORK_VOLUME}

envs:
  RCLONE_CONFIG_R2_TYPE: ""
  RCLONE_CONFIG_R2_PROVIDER: ""
  RCLONE_CONFIG_R2_ACCESS_KEY_ID: ""
  RCLONE_CONFIG_R2_SECRET_ACCESS_KEY: ""
  RCLONE_CONFIG_R2_ENDPOINT: ""
  WORKER_GIT_REF: ""
  SYNTH_SETTER_WORKER_RANK: ""
  SYNTH_SETTER_NUM_WORKERS: ""

setup: |
  set -euo pipefail
  echo "synth-setter worker ready (host: $(hostname))"
