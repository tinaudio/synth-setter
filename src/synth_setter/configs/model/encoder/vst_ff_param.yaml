defaults:
  - /model/backbone@_here_: ast_projection
  - _self_

_target_: synth_setter.models.components.vst_param_encoder.VSTFeedForwardParamEncoder
d_out: 512

projection:
  _target_: synth_setter.models.components.transformer.LearntProjection
  d_model: ${..d_model}
  d_token: ${..d_model}
  num_params: ${param_spec_width:${synth.param_spec_name}}
  num_tokens: 128
  initial_ffn: true
  final_ffn: false
