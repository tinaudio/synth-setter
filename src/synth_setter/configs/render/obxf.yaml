# Draft render config for obxf — generated by
# synth-setter-introspect-plugin. Generic render knobs inherit from the
# surge_xt group config; the fields below pin this synth's identity.
defaults:
  - surge_xt

plugin_path: "plugins/OB-Xf.vst3"
preset_path: "presets/obxf-base.vstpreset"
param_spec_name: "obxf"
renderer_version: "1.0.3"
