# surgepy backend knobs; identity comes from the root synth group (`synth=surge_xt_surgepy`).
defaults:
  - surge_xt
  - _self_

renderer_backend: surgepy
plugin_reload_cadence: render
gui_toggle_cadence: never
