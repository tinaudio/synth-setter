# surgepy backend knobs; identity comes from the root synth group (`synth=surge_simple_surgepy`).
defaults:
  - surge_simple
  - _self_

renderer_backend: surgepy
plugin_reload_cadence: render
gui_toggle_cadence: never
