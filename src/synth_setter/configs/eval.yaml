# @package _global_

defaults:
  - _self_
  - data: ??? # choose datamodule with `test_dataloader()` for evaluation
  - model: ???
  - logger: null
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default
  - callbacks: none
  - experiment: null
  - render: null

task_name: "eval"

tags: ["dev"]
mode: test

# Predict-mode post-processing; gated off so `mode: test`/`validate` stay unchanged.
evaluation:
  download_dataset_root_uri: null
  render_vst: false
  compute_metrics: false
  rerender_target: true
  num_workers: 1

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
