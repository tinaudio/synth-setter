# @package _global_

defaults:
  - kosc/base
  - override /model: ffn
  - _self_

run_name: ffn_sort
model:
  loss_fn: mse
  net:
    out_dim: ${mul:${datamodule.k},3}

datamodule:
  sort_frequencies: true
