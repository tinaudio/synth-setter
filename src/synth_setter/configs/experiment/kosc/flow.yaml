# @package _global_

defaults:
  - kosc/base
  - override /model: flow
  - _self_

run_name: flow

model:
  vector_field:
    projection:
      num_params: ${mul:${datamodule.k},3}

datamodule:
  ot: true
