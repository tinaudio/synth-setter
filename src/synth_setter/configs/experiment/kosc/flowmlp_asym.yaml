# @package _global_
defaults:
  - kosc/base
  - override /model: flowmlp
  - _self_

run_name: flowmlp_asym

model:
  vector_field:
    n_params: ${mul:${data.k},3}

data:
  break_symmetry: true
  ot: true
