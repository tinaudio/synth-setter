# @package _global_

defaults:
  - kosc/base
  - override /model: flow_fixed
  - _self_

run_name: flow_fixed_asym
model:
  vector_field:
    projection:
      params_per_token: 3

data:
  break_symmetry: true
  ot: true
