# @package _global_
defaults:
  - kosc/base
  - override /model: flow
  - _self_

run_name: flow_asym

model:
  vector_field:
    projection:
      num_params: ${mul:${datamodule.k},3}

datamodule:
  break_symmetry: true
  ot: true
