# @package _global_

defaults:
  - surge/base
  - override /model: surge_flowmlp
  - _self_

experiment_name: surge-simple-onehot
run_name: flowmlp

model:
  num_params: 92

callbacks:
  plot_proj_ii: null

# Pinned model-{config_id} artifact resolved lazily by the ${wandb:...} resolver.
ckpt_path: ${wandb:tinaudio/synth-setter/model-flow_mlp_simple:latest}
