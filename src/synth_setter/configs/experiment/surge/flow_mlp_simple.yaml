# @package _global_

defaults:
  - surge/base
  - override /model: surge_flowmlp
  - _self_

experiment_name: surge-simple-onehot
run_name: flowmlp

model:
  num_params: 92

callbacks:
  plot_proj_ii: null
