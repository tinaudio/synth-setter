# @package _global_

defaults:
  - surge/base
  - override /model: surge_flowvae
  - _self_

model:
  net:
    latent_dim: 92
  param_spec: surge_simple

experiment_name: surge-simple-onehot
run_name: vae

callbacks:
  plot_proj_ii: null
