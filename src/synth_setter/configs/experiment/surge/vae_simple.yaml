# @package _global_

defaults:
  - surge/base
  - override /model: surge_flowvae
  - _self_

model:
  net:
    latent_dim: 92
  param_spec: surge_simple

experiment_name: surge-simple-onehot
run_name: vae

callbacks:
  plot_proj_ii: null

# Pinned model-{config_id} artifact resolved lazily by the ${wandb:...} resolver.
ckpt_path: ${wandb:tinaudio/synth-setter/model-vae_simple:latest}
