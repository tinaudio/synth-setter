# @package _global_

# Dedicated CLAP conditioning run on the finalized 440k Surge Simple dataset.
defaults:
  - /experiment/surge/flow_simple_440k
  - override /conditioning: clap
  - _self_

run_name: flow_440k_clap_100k

trainer:
  min_steps: 100_000
  max_steps: 100_000
