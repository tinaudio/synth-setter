# @package _global_

defaults:
  - surge/base
  - override /model: surge_ffn
  - _self_

model:
  net:
    d_out: 92

experiment_name: surge-simple-onehot
run_name: ffn

callbacks:
  plot_proj_ii: null
