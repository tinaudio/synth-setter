# @package _global_

defaults:
  - surge/base
  - override /model: vst_flow
  - _self_

model:
  vector_field:
    projection:
      final_ffn: false

experiment_name: surge-simple-onehot
run_name: flow-nofinalffn
