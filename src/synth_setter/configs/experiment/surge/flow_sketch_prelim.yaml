# @package _global_

# Preliminary 1k sketch A/B, sketch arm (#2707): identical to the base arm
# except for the nested sketch-control conditioning.

defaults:
  - /experiment/surge/flow_sketch_prelim_base
  # Quoted: bare `on` is YAML boolean True, not the config-group name.
  - override /sketch: "on"
  - _self_

run_name: flow1k_prelim_sketch
