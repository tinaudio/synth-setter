# @package _global_

defaults:
  - /experiment/surge/base
  - override /model: vst_flowmlp
  - override /datamodule: surge
  - override /synth: surge_xt
  - override /callbacks: [default_vst, log_per_param_mse]
  - _self_

run_name: flowmlp
experiment_name: surge-full-onehot

tags:
  - surge

callbacks:
  plot_proj_ii: null
