# @package _global_

defaults:
  - surge/base
  - override /model: surge_flowmlp
  - override /data: surge
  - _self_

run_name: flowmlp
experiment_name: surge-full-onehot

model:
  num_params: 300

tags:
  - surge

callbacks:
  log_per_param_mse:
    _target_: synth_setter.utils.callbacks.LogPerParamMSE
    param_spec: surge_xt
  plot_proj_ii: null
