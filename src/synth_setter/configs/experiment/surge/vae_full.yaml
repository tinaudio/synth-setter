# @package _global_

defaults:
  - surge/base
  - override /data: surge
  - override /model: surge_flowvae
  - _self_

model:
  net:
    latent_dim: 300
  param_spec: surge_xt

experiment_name: surge-full-onehot
run_name: vae

callbacks:
  plot_proj_ii: null
