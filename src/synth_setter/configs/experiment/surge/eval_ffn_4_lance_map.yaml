# @package _global_

# surge/eval_ffn_4 on the native lance.torch map dataloaders: same predict-mode
# eval contract, only the loader switches off the legacy batch-indexed adapter.

defaults:
  - /experiment/surge/eval_ffn_4
  - override /datamodule: surge_lance_map
  - _self_

experiment_name: surge-4-toy-eval-lance-map
