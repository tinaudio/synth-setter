# @package _global_

defaults:
  - /experiment/surge/base
  - override /model: surge_ffn
  - override /datamodule: surge_lance_sharded
  - _self_

run_name: ffn
experiment_name: surge-4-toy
datamodule:
  param_spec_name: surge_4

model:
  net:
    # len(param_specs["surge_4"]): the spec's encoded width.
    d_out: 7

callbacks:
  log_per_param_mse:
    _target_: synth_setter.utils.callbacks.LogPerParamMSE
    param_spec: surge_4
  plot_proj_ii: null
