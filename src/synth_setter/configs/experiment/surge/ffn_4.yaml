# @package _global_

defaults:
  - /experiment/surge/base
  - override /model: vst_ffn
  - override /datamodule: surge_lance
  - override /synth: surge_4
  - override /callbacks: [default_vst, log_per_param_mse]
  - _self_

run_name: ffn
experiment_name: surge-4-toy

callbacks:
  plot_proj_ii: null
