# @package _global_

defaults:
  - fm/base
  - override /model: flow

run_name: flow_asym

model:
  vector_field:
    num_tokens: ${out_dim}
    projection:
      num_tokens: ${out_dim}
      num_params: ${out_dim}

data:
  break_symmetry: true
  ot: true
