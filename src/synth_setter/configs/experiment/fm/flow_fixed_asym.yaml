# @package _global_

defaults:
  - fm/base
  - override /model: flow_fixed

run_name: flow_fixed_asym

model:
  vector_field:
    num_tokens: ${out_dim}
    projection:
      k: ${div:${out_dim},2}

data:
  break_symmetry: true
  ot: true
