# @package _global_

defaults:
  - fm/base
  - override /model: flow_fixed

run_name: flow_fixed

model:
  vector_field:
    num_tokens: ${out_dim}
    projection:
      k: ${div:${out_dim},2}

data:
  ot: true
