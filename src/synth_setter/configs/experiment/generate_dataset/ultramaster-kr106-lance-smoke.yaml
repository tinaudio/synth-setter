# @package _global_

defaults:
  - override /datamodule: vst
  - override /synth: ultramaster_kr106
  - override /render: vst
  - _self_

task_name: ultramaster-kr106-lance-smoke
output_format: lance
train_val_test_sizes: [20, 0, 0]
mask_degenerate_bins: true
finalize_inline: true

render:
  samples_per_render_batch: 1
  samples_per_shard: 20
  max_retries: 20
  plugin_reload_cadence: render
