# @package _global_

defaults:
  - pyfdn/flow
  - override /synth: pyfdn_diffvox
  - override /render: pyfdn_diffvox
  - _self_

run_name: diffvox_flow
tags: [pyfdn, diffvox, flow]

model:
  encoder:
    input_channels: 2
