# @package _global_

# Preserve legacy datasets and pooling experiments on the four-second mel grid.
model:
  sketch_controls:
    column: sketch
    num_frames: 401
    num_control_tokens: 32
    pitch_zero_threshold: 0.1

datamodule:
  sketch: ${model.sketch_controls}
