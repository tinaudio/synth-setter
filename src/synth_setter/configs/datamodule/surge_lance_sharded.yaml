defaults:
  - surge
  - _self_

# dataset_root points at the dataset run directory itself (shard-*.lance +
# input_spec.json + stats.npz) — typically an `rclone mount` of the R2 run
# prefix; see docs/design/training-pipeline.md for the mount command.
_target_: synth_setter.data.lance_datamodule.ShardedLanceVSTDataModule
