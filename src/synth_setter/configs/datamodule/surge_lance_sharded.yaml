defaults:
  - surge
  - _self_

# dataset_root is the run directory itself (shard-*.lance + input_spec.json +
# stats.npz) — typically an `rclone mount` of the R2 run prefix.
_target_: synth_setter.data.lance_datamodule.ShardedLanceVSTDataModule
