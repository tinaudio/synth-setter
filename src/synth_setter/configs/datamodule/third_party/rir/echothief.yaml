defaults:
  - /datamodule/third_party/base@_here_
  - _self_

# Stereo WAV impulse responses only; the licence PDF is the one non-audio row.
dataset_uri: r2://experiments/third_party/EchoThief/all.lance
dataset_version: 1
audio_column: source_bytes
row_filter: "audio_decodable = true"
downmix: true
