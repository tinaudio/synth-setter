# Single-accelerator training pool. An any-of pool can place two arms of one A/B
# on different GPUs, which confounds the comparison, so this pins one type.
name: runpod-training-h100
resources:
  - cloud: runpod
    accelerators:
      H100-SXM: 1
    use_spot: false
    disk_size: 900
