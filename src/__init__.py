"""Legacy ``src`` package.

Post-Phase-2 the ML code (``data`` / ``models`` / ``utils`` / ``metrics``) has moved to
``src/synth_setter/``; only ``src/pipeline/`` remains here. ``src.pipeline`` itself
moves under ``synth_setter`` in Phase 3 (#784), after which this file and the
``src/`` directory are deleted.
"""
