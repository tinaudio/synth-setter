"""Legacy ``src`` package.

Post-Phase-2 it hosts only ``src.pipeline``;
the rest of the codebase has moved to ``src/synth_setter/``. ``src.pipeline``
itself moves under ``synth_setter`` in Phase 3 (#784), after which this file
and the ``src/`` directory are deleted.
"""
