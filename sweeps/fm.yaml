program: src/train.py
entity: benhayes
project: synth-permutations
command:
  - python
  - src/train.py
  - ${args_no_hyphens}
name: fm_sweep_flowonly
method: grid
metric:
  goal: minimize
  name: val/lsd
parameters:
  experiment:
    values:
      # - fm/flow
      # - fm/ffn_mse
      # - fm/ffn_asym
      # - fm/flow_asym
      - fm/flow_fixed
      - fm/flow_fixed_asym
  "experiment/fm/algorithm":
    values:
      - conditional
      - mixed
      - hierarchical
